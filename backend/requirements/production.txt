-r base.txt

# Production WSGI server.
gunicorn==26.0.0

# Serves collected static assets directly from the app process so
# production doesn't need a separate static file host or CDN just to
# get started.
whitenoise==6.12.0
